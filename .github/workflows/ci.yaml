name: CI

on:
  push:

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - run: |
          realpath .
          ls -lah .

      - id: monoversion
        uses: arrdem/monoversion@main
        with: {}
